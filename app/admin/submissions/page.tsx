import Link from "next/link";
import { redirect } from "next/navigation";
import { requireAdmin } from "@/lib/admin";
import { getAdminSubmission } from "@/lib/submissionAdmin";
import SubmissionList from "@/components/admin/SubmissionList";
export default async function AdminSubmissionsPage({ searchParams }: { searchParams: Promise<{ status?: string; page?: string }> }) { const supabase = await requireAdmin(); if (!supabase) redirect("/auth/admin-login"); const query = await searchParams; const status = ["PENDING", "APPROVED", "REJECTED"].includes((query.status ?? "").toUpperCase()) ? (query.status ?? "").toUpperCase() : ""; const page = Math.max(1, Number(query.page ?? "1")); const limit = 10; let request = supabase.from("submissions").select("id", { count: "exact" }); if (status) request = request.eq("status", status); const { data: rows, count } = await request.order("submitted_at", { ascending: false }).range((page - 1) * limit, page * limit - 1); const submissions = []; for (const row of rows ?? []) { const result = await getAdminSubmission(row.id); if (result.submission) submissions.push(result.submission); } const totalPages = Math.max(1, Math.ceil((count ?? 0) / limit)); const link = (nextPage: number) => `/admin/submissions?${status ? `status=${status.toLowerCase()}&` : ""}page=${nextPage}`; return <main className="min-h-screen bg-[#0b0b0f] text-white"><section className="mx-auto max-w-6xl px-6 py-12"><Link href="/admin" className="text-sm text-cyan-300">← Admin dashboard</Link><h1 className="mt-8 text-4xl font-bold">All submissions</h1><div className="mt-6 flex flex-wrap gap-2">{["", "pending", "approved", "rejected"].map((filter) => <Link key={filter || "all"} href={`/admin/submissions${filter ? `?status=${filter}` : ""}`} className={`rounded-full px-3 py-1.5 text-sm ${status.toLowerCase() === filter ? "bg-cyan-500 text-slate-950" : "border border-white/15 text-zinc-300"}`}>{filter || "all"}</Link>)}</div><div className="mt-6"><SubmissionList submissions={submissions} /></div><div className="mt-5 flex items-center justify-between text-sm"><Link href={page > 1 ? link(page - 1) : "#"} className={page > 1 ? "text-cyan-300" : "pointer-events-none text-zinc-600"}>Previous</Link><span className="text-zinc-400">Page {page} of {totalPages}</span><Link href={page < totalPages ? link(page + 1) : "#"} className={page < totalPages ? "text-cyan-300" : "pointer-events-none text-zinc-600"}>Next</Link></div></section></main>; }
