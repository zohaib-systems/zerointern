import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { getAdminSubmission } from "@/lib/submissionAdmin";
import ReviewActions from "@/components/admin/ReviewActions";
export default async function SubmissionReviewPage({ params }: { params: Promise<{ submissionId: string }> }) { const { submissionId } = await params; const { supabase, submission } = await getAdminSubmission(submissionId); if (!supabase) redirect("/auth/admin-login"); if (!submission) notFound(); return <main className="min-h-screen bg-[#0b0b0f] text-white"><section className="mx-auto max-w-4xl px-6 py-12"><Link href="/admin/submissions" className="text-sm text-cyan-300">← All submissions</Link><h1 className="mt-8 text-4xl font-bold">Review submission</h1><div className="mt-8 grid gap-5 md:grid-cols-2"><section className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Student</h2><p className="mt-4">{submission.userName ?? "Unnamed"}</p><p className="text-zinc-400">{submission.userEmail}</p></section><section className="rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Project</h2><p className="mt-4">{submission.projectTitle}</p><p className="text-zinc-400">{submission.trackTitle}</p></section></div><section className="mt-5 rounded-2xl border border-amber-400/30 bg-amber-400/10 p-6"><h2 className="font-semibold text-amber-200">Business problem</h2><p className="mt-3 text-zinc-300">{submission.projectProblem}</p><h2 className="mt-6 font-semibold text-amber-200">Brief</h2><p className="mt-3 whitespace-pre-wrap text-zinc-300">{submission.projectBrief}</p></section><section className="mt-5 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Submission details</h2><p className="mt-4 text-sm text-zinc-400">Submitted {new Date(submission.submittedAt).toLocaleString()}</p><div className="mt-4 flex flex-wrap gap-4"><a href={submission.repoUrl} target="_blank" rel="noreferrer" className="text-cyan-300">Repository ↗</a><a href={submission.liveUrl} target="_blank" rel="noreferrer" className="text-cyan-300">Live project ↗</a></div></section><section className="mt-5 rounded-2xl border border-white/10 bg-white/[0.04] p-6"><h2 className="text-xl font-semibold">Review actions</h2><p className="mt-3 text-zinc-400">Current status: {submission.status}</p><div className="mt-5"><ReviewActions submissionId={submission.id} status={submission.status} /></div></section></section></main>; }
