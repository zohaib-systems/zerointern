import Link from "next/link";
import type { SubmissionWithDetails } from "@/types";
interface SubmissionListProps { submissions: SubmissionWithDetails[]; }
export default function SubmissionList({ submissions }: SubmissionListProps) { if (!submissions.length) return <p className="rounded-xl border border-dashed border-white/15 p-8 text-center text-zinc-400">No submissions found.</p>; return <div className="overflow-x-auto rounded-xl border border-white/10"><table className="w-full min-w-[700px] text-left text-sm"><thead className="bg-white/5 text-zinc-400"><tr>{["Student", "Project", "Track", "Submitted", "Status", "Action"].map((heading) => <th key={heading} className="px-4 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{submissions.map((submission) => <tr key={submission.id} className="border-t border-white/10 hover:bg-white/[0.03]"><td className="px-4 py-4">{submission.userName ?? "Unnamed"}<br /><span className="text-xs text-zinc-500">{submission.userEmail}</span></td><td className="px-4 py-4">{submission.projectTitle}</td><td className="px-4 py-4">{submission.trackTitle}</td><td className="px-4 py-4 text-zinc-400">{new Date(submission.submittedAt).toLocaleDateString()}</td><td className="px-4 py-4">{submission.status}</td><td className="px-4 py-4"><Link href={`/admin/submissions/${submission.id}`} className="text-cyan-300">Review</Link></td></tr>)}</tbody></table></div>; }
