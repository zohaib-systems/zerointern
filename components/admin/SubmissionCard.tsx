import Link from "next/link";
import type { SubmissionWithDetails } from "@/types";
interface SubmissionCardProps { submission: SubmissionWithDetails; }
export default function SubmissionCard({ submission }: SubmissionCardProps) { return <Link href={`/admin/submissions/${submission.id}`} className="block rounded-xl border border-slate-600 bg-slate-800 p-4 transition hover:border-cyan-400/40"><div className="flex justify-between gap-3"><div><p className="font-medium">{submission.userName ?? submission.userEmail}</p><p className="text-sm text-slate-300">{submission.projectTitle} · {submission.trackTitle}</p></div><span className="text-xs text-slate-300">{submission.status}</span></div><p className="mt-2 text-xs text-slate-400">{new Date(submission.submittedAt).toLocaleDateString()}</p></Link>; }
